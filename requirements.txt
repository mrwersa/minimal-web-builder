# Minimal Web Builder — Python backend dependencies
# The backend is FastAPI + LangGraph; the frontend is React+Vite (see web/).

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
# LangGraph agent: stateful, conversational, guardrails, memory, resilience
langgraph>=1.0.0
langchain-core>=1.0.0
# Gemini provider (optional — OpenRouter is configured by default)
google-generativeai>=0.3.2
python-dotenv